{
  "author": "MBLabs (Squad Desenvolvimento)",
  "version": "1.2.0",
  "name": "@bankeiro/sdk-corecard-grecale",
  "main": "dist/corecard/sdk.js",
  "types": "dist/corecard/sdk.d.ts",
  "license": "MIT",
  "scripts": {
    "co:login": "aws codeartifact login --tool npm --repository bankeiro-backend --domain bankeiro --domain-owner 367063506873 --region us-west-2",
    "build": "rm -rf ./dist && npx tsc && tsc-alias",
    "test": "jest --config jest.config.ts --runInBand --passWithNoTests",
    "test:coverage": "jest --config jest.config.ts --runInBand --passWithNoTests --coverage",
    "lint": "npx biome lint --write"
  },
  "dependencies": {
    "@bankeiro/bankeiro-backend-corecard": "^2.3.0",
    "@hyperflake/http-errors": "^1.0.0",
    "axios": "^1.10.0",
    "tsc-alias": "^1.8.16"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.0.10",
    "biome": "^0.3.3",
    "jest": "^30.0.3",
    "ts-jest": "^29.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
