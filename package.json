{
  "name": "@bankeiro/sdk-corecard-grecale",
  "version": "1.1.0",
  "main": "dist/src/corecard/sdk.js",
  "types": "dist/src/corecard/sdk.d.ts",
  "author": "MBLabs",
  "license": "MIT",
  "scripts": {
    "co:login": "aws codeartifact login --tool npm --repository bankeiro-backend --domain bankeiro --domain-owner 367063506873 --region us-west-2",
    "co:login:dev": "aws codeartifact login --tool npm --repository bankeiro-backend-dev --domain bankeiro --domain-owner 367063506873 --region us-west-2",
    "build": "rm -rf ./dist; npx tsc && tsc-alias",
    "test": "jest --config jest.config.ts --passWithNoTests",
    "test:unit": "yarn test --runInBand --verbose",
    "test:coverage": "jest --coverage --passWithNoTests jest.config.ts",
    "test:debug": "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand",
    "test:e2e": "jest --config ./test/jest-e2e.json",
    "lint": "echo disabled",
    "lint:disabled": "npx @biomejs/biome lint --write",
    "lint:fix:disabled": "npx @biomejs/biome lint --fix"
  },
  "dependencies": {
    "@bankeiro/bankeiro-backend-corecard": "file:../sdk-interfaces",
    "@hyperflake/http-errors": "^1.0.0",
    "axios": "^1.10.0",
    "tsc-alias": "^1.8.16"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.0.10",
    "jest": "^30.0.3",
    "ts-jest": "^29.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
